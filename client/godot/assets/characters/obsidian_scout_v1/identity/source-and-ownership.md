# 来源、权属与替换路径

- `characterId`: `obsidian_scout_v1`
- 本人物、身份板、独立头像、透明全身展示、世界帧与战斗动作均为 Beastbound Odyssey 原创 AI 辅助制作。
- 原始图像仅使用 OpenAI 内置图像生成；没有输入、拼接、描摹或复制任何外部商业人物美术。
- 用户提供的石器时代·觉醒截图只用于理解角色创建界面的布局、镜头比例与交互意图，不作为角色图像输入或身份来源。
- 固定身份依据为 `identity/identity-lock.md` 与 `identity/identity-board.png`；所有动作生成都必须引用二者。
- 透明图采用同次生成原图的纯色背景及其精确背景移除过程；禁止对已经透明、缺少同次 eligibility mask 的图执行推测式全局去色。
- 运行帧由规范化源帧确定性派生，禁止镜像、补间造帧、复制别人人物帧或使用程序图元替代人物美术。
- 替换路径：沿用身份锁重新生成指定动作原始网格，保留新 prompt/raw/处理元数据和哈希，通过身份、方向、边界、动作语义、KO 连续性与实机验收后再替换。
- 当前状态只能保持 `owner_review_pending`；自动 QC 或代理自评不得改为 `approved`。
