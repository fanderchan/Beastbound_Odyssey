Use case: precise-object-edit
Asset type: production source sheet for deterministic chroma-key extraction
Primary request: preserve the six Earth Vein Cave tile top surfaces and their exact 3-column by 2-row order, but replace everything outside each tile with one perfectly flat, uniform solid #FF00FF background.
Keep unchanged: six tile identities, clean hand-painted HD style, material colors, same 1536x1024 canvas, same equal 512x512 cells, same 2:1 isometric diamond perspective.
Required changes:
- Entire non-tile background must be exactly one uniform bright magenta #FF00FF with no gradient, texture, vignette, transparency, noise, bloom, haze, glow, drop shadow, ambient shadow, or color variation.
- Remove all light halos around the tiles.
- Keep every complete tile centered well inside its cell with at least 48 pixels of pure #FF00FF clearance from every cell edge.
- Thin ground diamonds only, no tabletop side-wall thickness.
Constraints: exactly six non-empty cells; tile surfaces fully opaque; sharp clean boundary suitable for chroma key; no text, grid lines, borders, labels, UI, actors, props, or watermark.
