# 芽耳布伊正式双视角补全动作生成记录

## 固定生成合同

- 工具：OpenAI 内置图像生成。
- 身份参考：`identity/identity-board.png`、既有 `qa/qa-contact-sheet.png`；`revive` 额外参考正/背视角 `down-8`。
- 画风：原创高清手绘 2.5D RPG 精灵；清晰轮廓、克制体积阴影、非像素风。
- 身份锚点：暖砂金毛、两只圆耳及耳尖嫩芽、浅色眉点、奶油色脸胸、橄榄叶脉背纹、棕色根芽尾和尾叶、四只短爪。
- 方向：`front_3quarter_sw` 面向左下；`back_3quarter_ne` 背向玩家并面向右上。
- 布局：每格单独完整主体，同尺度、稳定基线、中央安全区；不得越格、镜像、拼层、遮罩或加入骑手。
- 背景：纯色 `#FF00FF`；无地面、阴影、文字、边框、网格、水印和分离特效。

## 动作节拍

- `skill`：警觉站姿 -> 叶脉收拢蓄力 -> 前扑 -> 叶能头爪合击峰值 -> 回弹 -> 复位。生成源：`exec-5232afe9-8c93-4b0b-b282-f7b47a716343.png`。
- `dodge`：预判 -> 压低后腿 -> 横向弧线闪身 -> 腾空躲避 -> 落地 -> 复位；最终选用每视角四个最完整阶段。生成源：`exec-52a51b29-936d-424b-be64-3a76b29f3494.png`。
- `counter`：反应/支撑 -> 锁定攻击者 -> 爆发起步 -> 头爪反击 -> 回弹 -> 复位。生成源：`exec-a9ebea91-fe59-46a5-a7af-e505edaeedf9.png`。
- `knockaway`：受击压缩 -> 离地 -> 空中翻滚 -> 飞离；惊愕但不死亡。生成源：`exec-9dc2cfe2-0808-46df-84a0-cccee11391ec.png`。
- `revive`：昏厥保持 -> 睁眼 -> 前爪撑起 -> 稳定站立；不瞬移、不把光环画进本体。生成源：`exec-3bd80c29-00e2-4a7f-a013-2ebcbad807e4.png`。

## 后处理

只执行整图色键去除、连通主体提取、等格重排、统一缩放、底部/中心锚定和 512px -> 256px 归一化；没有绘制新姿势、重画部位、人物宠物拼接、镜像补方向或遮罩修缝。原始无损 WebP、重排元数据、管线元数据、512px 源帧和联系表均保存在 `source/formal-v2/` 与 `qa/formal-v2/`。
