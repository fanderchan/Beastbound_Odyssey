# Phase203：底部功能栏新手教学闭环

## 完成内容

- 主线主要节点由 25 个扩展为 39 个，把功能教学穿插在骑宠、补给、首战、装备精灵、组队和捕捉阶段，不在开局连续堆叠按钮任务。
- 新增可验证目标：打开功能、商店出售、开始挂机、交易所上架、领取指定邮件、交易所购买、附近聊天。
- 底栏覆盖闭环：状态、背包、装备、宠物、图鉴、任务、地图、聊天、队伍、家族、买卖、邮箱、内挂、账号、挂机。
- 战斗中挂机停止已请求后，底栏按钮立即从“停”恢复为“挂机”，明确表示本场结束后不会继续挂机。

## 买卖教学

1. 喂肉任务奖励 2 张“旧兽皮”。
2. 玩家先向杂货铺出售 1 张，学习商店出售页。
3. 剩余 1 张在交易所上架；仅当前教学任务、数量 1、石币单价 1–20 时，教学买家才会立即购买。
4. 成交款进入带 `tutorial_market_sale` 类型的教学邮件，玩家必须打开邮箱领取。
5. 领取后只为该账号显示一条“新手交易指导员”的 1 石币肉挂单；购买成功后挂单消失且不能重放。

教学挂单不进入公共持久化交易列表，普通玩家互相交易、税率和成交邮件保持原行为。

## 验证证据

- Node 针对性测试：`auth-quest-hang`、`auth-economy`、`auth-social-world`、`auth-http-server` 共 58/58；补充 `auth-auth-session`、`auth-profile-actions`、`auth-storage` 共 38/38。
- Godot 针对性检查：14/14，通过任务全链、目标模板、任务 UI、任务路线、NPC 标记、挂机设置/闭环、交易所、邮箱、聊天、商店和服务器档案同步。
- 性能：改前 idle `process_total` 中位约 0.40ms、p95 约 0.47ms，改后中位约 0.43ms、p95 约 0.50ms；moving 由中位约 0.32ms 降至约 0.23ms。移动连点 `max_input_us=143`、`coalesced=true`、`settled=true`；商店选择性能检查通过。
- PC 1280×720 预览截图：`.run/visual/phase203_tutorial_task.png`，确认“卖给其他玩家”目标、奖励、行动文本和自动寻路按钮无截断。

未运行完整 `node tools/run_local_ci.mjs`；本阶段使用覆盖改动域的窄测试，避免触发本机 MySQL 压力与大体量 binlog。
