# Phase 578：合并地图相机策略判断

日期：2026-09-20。继续 R1.W024。将重复的地图相机资格判断集中到 `WorldPresentationProfile` 的一个入口，保持已有比例、构图、候选边界和坐标转换。

## 实现与行为边界

此前查询 HUD 地标构图时，依次检查 Firebud 和 Earth Vein，重复读取 active、bundleId、候选标记和状态。现在先确定地图类型，再执行该类型的规则；缩放、锚点、HUD 构图与端点安全范围共用这一策略入口。每次读取当前字段，没有增加需要失效的缓存。

- Earth Vein：正式地图仍用 1.52；待审候选仍要求完整候选标记及显式预览，端点资格保持原有语义。
- Firebud v2：仍是显式预览下的 1.82 和 40/60 横向构图，单独把 status 改成 released 不会开放普通路径。
- 非活动、未知地图和不完整候选继续回退到原有默认相机；没有修改地图生命周期或发布规则。

## 行为验证

扩展已有相机策略检查：3 类地图 × 4 个状态 × 2 种 ID 空白写法 × 16 种标记组合，共 **384 种状态**。同一字典原地切换状态，核对缩放、锚点、HUD 构图、端点资格及输入字典不被修改。原有 Main 换图、战斗进出和 screen/world 往返检查继续运行。

```sh
node tools/run_godot_auto_checks.mjs \
  --only=--auto-world-presentation-profile-check,--auto-map-visual-runtime-check,--auto-camera-click-check \
  --fail-fast --timeout-ms 180000 \
  --output-dir .run/godot_auto_checks/phase578-camera-policy-final
```

最终解析及定向检查 **4/4 通过**，见 [完整回执](../.run/godot_auto_checks/phase578-camera-policy-final/2026-09-19T17-32-15-412Z_summary.json)。

## 前后测量

基线保存自 `c0893047ac277c83632a4ffa85c4020cbcd94be9`。隔离副本只将 Main 的策略预加载指向旧文件；美术、人物、场景及其余代码相同。主目录未切回旧版，源文件哈希随对照保存。

12 个独立 headless Main，静止／移动各三次前后交替，预热 180 帧、测量 480 帧。下表是每组运行均值的中位数，单位 ms：

| 区段 | 静止前 → 后 | 移动前 → 后 |
| --- | ---: | ---: |
| 相机 | 0.002250 → 0.002000 | 0.013500 → 0.013375 |
| Main `process_total` | 0.020250 → 0.020125 | 0.036375 → 0.035875 |
| 完整 `process_scope_total` | 0.028125 → 0.028000 | 0.057250 → 0.056625 |

整体变化很小，应视为接近持平，不能宣称整游戏帧率或 CPU 提升。六组移动各完成 60 次跨帧左键点击、120 个事件，接受数、最终目标及屏幕投影全部正确。原始数据见 [最终场景对照](../.run/phase578-headless-final/)。

[局部策略测量](../.run/phase578-policy-cost-final/) 每种场景前后交替七轮，每轮 20,000 次查询。网格／Earth Vein／Firebud 的中位数分别为 `0.35410→0.15605`、`1.03125→0.47350`、`0.38125→0.39265` 微秒。Earth Vein 的重复判断明显减少；Firebud 仍有很小的增加，不能把这组局部数据推广成所有地图性能改善。

## 原生窗口验证

取得一段最终源码的正常时钟原生二层静止诊断：前台预热 8 秒后观察 20 秒，**600 个观察帧全部实际绘制，零失焦、零不可绘制**，30 FPS 上限、127 个 draw calls。20 次累计 CPU 采样计算得到约 **7.816%**。见 [单次诊断及清理记录](../.run/phase578-window-move-diagnostic/)。这是当前源码的一段有效运行，不是完整前后对照或四层验收，不能与不同轮次的 7.784% 直接相减作为优化收益。

其余原生前后尝试未越过连续前台预热，全部拒绝，未拼接样本。最终 [无干预对照](../.run/phase578-native-final/) 和 [窗口恢复对照](../.run/phase578-native-window-recovery/) 均没有进入任何测量段。恢复对照在确认进程与窗口已初始化后读取窗口并尝试轻移标题栏；工具返回 `noWindowsAvailable`，随后原始记录仍未满足连续绘制条件。保留所有失败目录，不安装为正式性能证据。

[更细的窗口诊断](../.run/phase578-window-diagnostic/) 记录到：窗口可见、未最小化、渲染循环开启，实际窗口 ID 与默认焦点查询一致；恢复焦点后 `canDraw` 仍为 false，绘制帧数停在 43。不能只凭窗口截图或焦点字段认定测试有效。曾观察到窗口工具选中另一份测试专用项目管理器；该无参数测试进程已按 PID、完整命令和启动时间核对后回收，用户原有 Godot 进程保留。没有把它认定为全部失败的原因，也没有改变系统电源／安全设置。

所有本阶段进程、音频、预取和官方隔离目录均已清理，真实玩家目录哈希不变。已请求项目所有者在方便时协助保持前台，再启动集中复验；这不代表请求美术批准或放宽门槛。

## 保留与后续

未改变碰撞、遮挡、输入、比例、美术、服务端或协议。原有 107 项候选修改保留，111 项保护文件逐一核对不变。执行 `git diff --check` 和 `node tools/repository_guide.mjs refresh` / `check`，未运行全量 CI。

局部热点整理已完成；下一步先用当前源码完整原生矩阵确认剩余缺口，再补精确动作／鼠标配对和联网守护战操作。Phase 576 仍是最近一份完整矩阵，其静止增量失败结论不能由本次小范围结果覆盖；R1.W024、P2.1a、所有者接受及发布仍未完成。
