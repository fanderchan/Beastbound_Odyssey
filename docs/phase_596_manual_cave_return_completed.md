# Phase 596：守护战后完整鼠标返村

日期：2026-09-21。继续 R1.W024。在 Phase 595 的人工录制时钟条件下，通过真实 Computer Use 完成守护战胜利、F4→F3→F2→F1→火芽村口，并在村口再次点击移动。本轮没有修改客户端、服务端、玩法、素材或性能门槛；补齐的是此前未完成的完整鼠标返程。

## 操作范围与实际结果

基线为 `64713aa8ae3cd79413778e12664679ccaf7d56aa`，采集期间 656 个客户端脚本、共享数据、服务端及相关工具文件逐字节未变。沿用独立内存后端、一个真实 Main、四个 HTTP 测试队友和 10400 HP 路线人物；宠物正常受伤，没有途中补血、直接结算或传送。未使用 `--autoplay`。

四层通过小地图选择守护兽、点击挑战；前两回合分别给人物和战宠下达防御，第三回合开启游戏内自动战斗。胜利后通过地图出口逐层寻路。三层第一场遭遇继承自动开关，取消自动后因操作间隔超过指令期限而超时；此后五场遭遇均点击人物栏的正常逃跑按钮。每次确认结算后重新选择地图出口。

| 地图 | 实际结果 | 服务端已结算回合数 |
| --- | --- | ---: |
| 四层 | 守护战胜利 | 9 |
| 三层 | 取消自动后指令超时 | 1 |
| 三层 | 正常逃跑 | 1 |
| 二层 | 两场正常逃跑 | 2 |
| 一层 | 两场正常逃跑 | 2 |

7 个关闭房间共 15 次权威回合；Main 的 15 次开始、15 次完整动画结束与其逐项匹配，无漏播或重复。超时房间的第二回合指令没有结算，不能把下一回合编号当作已完成回合数。该流程是守护战胜利后的完整返程，**不是全胜返程或正常难度验收**；全胜自动路线另见 [Phase 592](phase_592_cave_return_playthrough.md)。

五个账号各获得地之戒 `+1`、石币 `120→379`，最终档案版本依次为 `108 / 107 / 107 / 108 / 106`，起点均为 `102`。不要求不同账号的合法结算写入次数相同。

## 画面、连接与原始操作

- 真实鼠标第 54 次调用显示火芽村口，第 55 次再次取得更新画面；第 56 次点击地面后坐标由 `(21,6)` 变为 `(21,3)`，队伍仍可移动。
- 1062 次洞穴世界采样均保留候选贴图、`earth_vein_cave_visual_v1` 和 `1.52×` 镜头；村口不是洞穴，不套用洞穴贴图断言。
- 886 次战斗采样包含 129 次关闭房间后的动画观察，均保留正确战场和已准备的纹理。
- 2027 次 ready 采样保持一条健康事件连接、零重试，没有人为制造网络中断。
- 2028 次窗口采样全部可绘制、渲染循环开启；1810 次聚焦，218 次未聚焦。本轮无不可绘制补绘或原生停画，但不能据此声称各种系统窗口条件下都已修复。

原始 Computer Use 共 56 次调用、54 张图片，零工具错误、零重置，含一次接口说明恢复。完整保留取消自动、超时以及所有逃跑过程。原始调用 SHA-256 为 `1932544a759e548a882acaca08312765963d8d5ff6e28c34b03df47af2c9e9aa`，见 [操作索引](../.run/phase596-manual-cave-return/computer-use-index.json)。图像从原始工具输出直接提取并逐张校验，未以运行时内部截图替代鼠标画面。

## 录制与核对

[完整录像](../.run/guardian-review/20260920T172415.899593Z/guardian-1x.mp4) 为 `1280×720 / 30 FPS / 32510 frames / 1083.666667s`，源包时间线、全片音视频解码和帧数均通过。SHA-256 为 `8fdec1bc2f6a59ecbbf5ca9f537d827c625e73214735fdb1ca5f9635b1b76d5b`。

限速器覆盖 32510 帧，实际时长 `1124.818103s`，单调时钟最短间隔 `33341µs`，最长间隔 `2.965303s`；原有 `33334µs` 门槛通过。慢帧使墙钟长于原始引擎时间线，没有把录像重新定时为墙钟长度。实际玩法区段 32448 个 process 帧的绘制连续性通过，缺失和后台补绘均为零；其计数边界与包含初始化／清理的完整影片不同。

临时离线汇总最初错误要求低频 Unix 时间戳的相邻帧差商全部小于 30；3 个间隔为 `30.011–30.225`，因此断言失败。状态是在帧内工作之后由定时器采样，采样相位和 Unix 时钟并非单调帧边界；修正后保留该诊断值，使用既有逐帧单调时钟回执验收限速。正式验证器、阈值、原始数据和运行源码均未改。失败记录见 [原始离线断言记录](../.run/phase596-manual-cave-return/offline-validation-initial-failure.json)。不能把这轮录制或采样商当作游戏 FPS、正常进程 CPU 或正式地图性能成绩。

本轮客户端、后端、录制／编码进程均正常退出，官方 QA lane 已清理，真实玩家资料哈希未变；原有 107 项候选修改及 111 项保护文件保持原字节，用户原先的 Godot 进程保留。完整结果见 [验证摘要](../.run/phase596-manual-cave-return/verification-summary.json) 与 [运行目录](../.run/guardian-review/20260920T172415.899593Z/)。

```sh
python3 tools/play_guardian_review.py --godot '.run/phase584-native-review/Beastbound QA.app/Contents/MacOS/Godot' --cave-journey --record --timeout-seconds 1800
python3 .run/phase596-manual-cave-return/extract_cua.py
python3 .run/phase596-manual-cave-return/validate.py
git diff --check
node tools/repository_guide.mjs refresh
node tools/repository_guide.mjs check
```

当前运行源码未变，未重复解析、单元测试或完整性能矩阵；既有 Phase 595 解析 `1/1`、Python `15/15` 仅作为该源码的前阶段测试记录。完整鼠标返程现已补齐，下一步继续正式静止增量与精确地图动作／截图配对。Phase 579 性能 **FAIL** 仍有效，P2.1a／R1.W024 及所有者美术接受仍未完成；一个客户端加 HTTP 队友不代表五真人联机、容量或正式发布。
