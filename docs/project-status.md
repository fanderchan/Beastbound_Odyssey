# 项目现状与下一步

核对日期：2026-09-19。日常开发统一使用 **`/Users/fander/projects/Beastbound_Odyssey`**。老板只需要从这个目录打开项目和本文；版本整理、实现、测试和文档同步由开发者负责。

## 游戏已经有什么

万兽纪元是 PC 优先、中文、始终在线的原创 2.5D 回合制宠物 MMORPG。核心体验是抓宠、练宠、观察隐藏成长，再通过转生、进化和融合追求稀有结果。

仓库已经实现账号和四角色槽、地图移动与任务、背包和装备、商店和资产交易、战斗与捕捉、挂机、宠物成长与转生/进化、队伍/聊天/邮件、家族庄园的基础系统。Node 服务端承担玩法权威，MySQL 是正常运行存储；客户端负责输入、表现和同步。这些是代码能力，具体功能是否开放、是否达到首发质量仍由发布计划和运行时开关决定。

目前是有完整系统基础的开发版，**尚未达到可收费上线的成品标准**。剩余方向包括正式视听与首发内容、核心循环和长期经济、Boss/PvP、社交/新手体验、真实支付、生产运维、200 人同图容量证据、桌面正式包和封测。不能用已关闭 Bug 数量或测试通过数量计算“上线完成率”。

## 版本已经统一

过去的多个目录是同一个 Git 仓库的多个工作区，用于隔离开发和发布验证。先前“多 60 个提交”指两个版本之间的历史差距，不是多出 60 份游戏。

本次已逐项核对并保留原有成果，把较新的 60 个提交、候选目录的 133 个未提交文件，以及本次文档整理合入原来的主目录。整合起点是 `0bd9ea705`；后续提交和未提交修改以实时 Git 状态为准。没有另建第四个开发目录。

| 目录 | 当前用途 |
| --- | --- |
| `Beastbound_Odyssey` | 唯一日常开发入口，包含统一后的代码、数据、工具和文档 |
| `Beastbound_Odyssey_release_candidate` | 整合前的历史现场，保留并加 Git worktree 保护锁，不再继续开发 |
| `_codex_worktrees/Beastbound_Odyssey_p14_release` | 旧融合专项历史现场；补丁比对未发现候选主线缺少的独有实现，保留并加保护锁 |

保护锁用于阻止 Git 自动清理或移动旧工作区，不是文件只读权限。后续任务不要在旧目录继续写入，也不要自行删除它们。

整合前两边共 390 个文件的原始字节、状态、SHA-256、补丁和分类决定保存在本机忽略目录 `.run/workspace-unification-20260917/`。原主目录还保留完整 Git stash 和 `refs/backup/workspace-unification-20260917/` 恢复引用。历史来源、资产原稿、冻结证据和玩家资料均保留。

```sh
node tools/repository_guide.mjs status
git status --short --branch
git log -5 --oneline
```

`status` 只读本地状态，不拉取、切分支、合并或启动服务。本次整合前已另行执行远端 fetch 并确认基线；未提交文件数不是未完成任务数，也不代表可发布。

## 当前开发顺序

产品范围由 [总路线图](../stoneage_gap_plan.md) 负责，逐步交付由 [生产发布计划](../production_release_loop_plan.md) 负责。两者现在都以主目录为准，旧目录的游标不再驱动开发。

1. **统一基线已恢复**：历史成果和文档导航已核对；完整服务端 1984 项通过、0 失败、1 项因未配置独立 Valkey 环境跳过，客户端定向 8/8 通过。
2. **一场守护战已形成可试玩、可审看的候选**：正式客户端已接入已有宠物动作和原创洞穴战场背景，修复地标挡人及人物倒下后漏交宠物指令。最新 [Phase 557](phase_557_current_cave_and_guardian_review.md) 已用真实鼠标完成挑战、人物攻击、宠物技能、蓄力防御、胜利返回和背包奖励检查；一个真实 Main 加四个 HTTP 测试队友，五账号均 `revision 102→103`、地之戒 `+1`、石币 `+256`。这不是五真人或平衡验收，远端世界队友仍有简化占位。试玩命令及美术来源见 [Phase 547](phase_547_earth_guardian_battle_presentation.md)。老板美术接受与四层完整性能仍待完成，发布游标保持 `R1.W024`。
3. **首次切战卡顿已明显改善，代码已同步 GitHub**：[Phase 548](phase_548_battle_hotpaths_and_authoritative_completion.md) 修复了人物击飞后提前胜利并减少绘制开销；[Phase 549](phase_549_battle_texture_prefetch_and_github_sync.md) 增加有界后台贴图预取，同一五人守护战的新进程首次准备由约 1002 ms 降到两轮约 10 ms。两场权威胜利与五账号奖励到账通过，严格原生前台性能两次通过，最终世界性能探针 `5/5`。此前各阶段已按功能提交推送 GitHub main；107 项待重冻地图证据继续在本地保留。未知形态或立即开战仍可能走同步加载，不能把这个场景的结果推广成全游戏无卡顿。
4. **依次推进成品门槛**：继续 R1.W024 的当前源码精确地图证据、真实操作、集中式重复移动性能和路线/战斗转换验证，再推进剩余视听验收、首发内容、玩法/经济、运营基础设施、正式构建和封测；每次交付都带具体效果或可复现结果。

地图性能工具已集中为一个窗口、48 个独立 Main 样本，并明确标注旧网格基线与当前美术候选，避免把对比画面误认作游戏退化。[Phase 550](phase_550_single_window_map_performance.md) 的工具回归通过，但二层已有静止增量超标记录，最终前台重测又因失焦被正确拒绝；当前不能宣称四层性能已通过，正式地图证据继续待重冻。

[Phase 551](phase_551_map_evidence_commit_provenance.md) 修复了仅提交文档/证据也会让同内容截图失效的审计误拒，`47/47` 通过；记录来源须为本仓库祖先提交，运行内容及独立资源校验仍必须一致。第二轮实机在第 10 组失焦后安全停止，前台局部诊断仍显示二层开销差异，完整重复性能结论继续待补。

[Phase 552](phase_552_camera_composition_work_reduction.md) 省掉相机已达到最优构图时的候选搜索和重复评分。64 组实际移动输入、1000 组随机边界输入的镜头结果完全一致，评分调用减少 18.1%，配对算法计时约减少 14.3%；解析、相机、点击回归 `3/3` 通过。二层 headless 移动相机区段 `0.0215 → 0.0159ms`，静止基本不变；这不替代原生性能验收，二层静止差异仍待解决。

[Phase 553](phase_553_battle_experience_feedback.md) 核对守护战经验归属并修复旧战报缺少击杀记录时仍猜测零经验原因的问题；显示逻辑回归已有呈现模型。客户端 `4/4`、服务端经验规则 `3/3`、结算飘字八项检查通过。Phase 552 推送后的原生地图采样第一组又失焦并安全退出，稳定前台性能和正式精确证据继续待补。

[Phase 554](phase_554_review_render_continuity.md) 修复后台窗口停止绘制但录像持续写入旧画面的问题，限于审片入口并逐逻辑帧验证绘制。当前四层/双地标原生与录像各 `9/9`，导出 64.4 秒四层片和 8.333 秒地标片；20 项自动动作截图完成且原证据已备份。工具 `28/28`、像素诊断和完整录制通过；审片明确不提供性能结论，前台性能、当前真实鼠标操作和候选整体验收仍待完成。

[Phase 555](phase_555_world_prop_player_visibility.md) 修复普通岩石完全挡住玩家的问题，复用动作轮廓与建图缓存排除透明边距的误判。最终定向 `4/4`、前后性能各 `5/5`，真实鼠标完成四层 20 项操作观察；107 项待重冻资产文件保持原字节。实机另发现 F4 守护兽导航停格 `(20,8)` 的地标顶部裁切，下一步先修正这条实际交互路线的构图，再统一重录正式画面与前台重复性能；本轮观察尚未安装为正式候选配对证据。

[Phase 556](phase_556_guardian_navigation_camera.md) 已修复上述寻路到达裁切，以及展开消息窗后的同类问题：纠正普通物件意外取得交互优先级，并避免通过裁切地标减少 HUD 预留间距的重叠面积。原生端点／实际导航 `12/12`、定向 `4/4`、前后通用性能各 `5/5`；真实鼠标验证对话开关、消息窗展开和收起。人物与两座交互地标完整显示，外围岩石／岩柱的覆盖仍如实记录。下一步重录当前源码的正式画面、动作配对与 48 样本前台性能，候选尚未发布。

[Phase 557](phase_557_current_cave_and_guardian_review.md) 已重新取得当前版本的 20 对自动动作截图、四层／地标原速片和碰撞回执，并将四层 20 项真实鼠标前后对照安装到本地候选；四层往返及守护者消息面板构图通过。原有 107 项证据已完整备份，五人守护战也完成当前版本鼠标复验。性能准备现要求连续 1 秒前台后才开始，但两轮仍因采样失焦拒绝；整包审计的 96 项错误均为旧性能回执工具哈希失效，不能称整包通过。其他地图如潮回洞穴仍有网格占位，不在岩脉四层完成范围内。

Firebud v2、融合、环境声和 Bui VFX 已有返工或延期决定；不要从旧主目录的 `R1.01` 重做。Earth Vein 仍是待验收候选；测试通过和受委托内部审查不会自动成为老板亲自批准的精确美术资产。

[Phase 558](phase_558_native_performance_visibility.md) 复现了“仍有焦点但窗口不可绘制”的性能采样缺口，新增准备页点击、持续可见及实际绘制帧检查。工具 `10/10+11/11`、解析 `1/1`；原生前 8 组有效，第 9 组因 98 帧不可绘制被拒绝，9 个 Main 和隔离目录均回收。二层有效局部数据仍显示当前美术静止 `0.520125ms`、移动 `0.699250ms` 超过既有 process scope 门槛，下一步继续定位热点并补齐完整重复矩阵，不能把问题都归因于窗口失焦。

[Phase 559](phase_559_world_depth_and_bounds_hotpaths.md) 精简人物范围变换与静止深度排序，保持比例、相机及遮挡规则；定向 `4/4`、前后通用性能各 `5/5`。本次终于取得完整的 48 组前台可绘制原生数据，四层绝对处理耗时均达标，但四层静止增量及二三层移动增量仍超门槛，总评 FAIL。107 项候选证据原字节保留，新的源码需要重新冻结画面。鼠标复核遇到 Mac 锁屏，另发现 scratch 动作入口被历史正式事务检查误阻；后续继续处理这些具体剩余项，并复核正常运行的进程 CPU，不能把脚本计时当作整机性能结论。

[Phase 560](phase_560_map_capture_transaction_history.md) 已修复上述取证入口阻断：临时检查与正式恢复分开，已完成历史保留，未完成事务仍严格校验后恢复。工具回归 `41/41+3/3`，两个新增用例在旧源码上复现失败；真实历史扫描通过，107 项候选及四项历史文件原字节未变。scratch 已到达隔离启动步骤，锁屏期间未启动窗口或伪称截图通过。完整数据指向移动相机构图的额外开销，下一步继续定向优化和正常窗口复核。

全局发布结论仍为 **BLOCKED**。R0 曾有完整候选验证历史，但不能把历史通过、当前开发目录和正式发布混为一谈。

## 老板与开发者怎么配合

老板给方向和最终效果反馈；开发者负责构思、拆解、实现、验证、整理和持续推进。常规技术选择、可逆修复与既定路线内工作直接执行，不再把 Git、目录或工具细节交给老板决策。

2026-09-18 老板已授权设定目标后连续自主开发，并要求已完成的工作提交 GitHub。持续目标是推进到可完整试玩的 PC 成品候选；当前先收口岩脉洞穴和五人守护战，再按既定路线推进。开发者连续执行实现、实机验证、文档和分批提交，不再逐阶段询问是否继续。该授权不等于最终美术验收或生产发布批准。

涉及尚未确定的收费/经济规则、重大产品取舍、外部账号与资源，以及最终视觉和封测接受时，先准备具体可看的结果，再说明推荐方案和影响，请老板决定。没有这些门槛时继续完成后续已授权工作。

开发方法见 [开发指南](development.md)，代码位置见 [架构说明](architecture.md)，整理过程见 [Phase 543](phase_543_repository_navigation_and_http_boundaries.md)，统一过程见 [Phase 544](phase_544_unified_development_workspace.md)。
